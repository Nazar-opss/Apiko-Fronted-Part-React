export default function GetItemDescription(props){
    return <p style={{color: 'green', fontWeight: 'bold', fontSize: '20px'}}>{props.nickName} kills people who doesn’t learn React with {props.weapon}</p>            
}